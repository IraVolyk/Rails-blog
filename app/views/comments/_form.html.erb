<%= form_with(model: [@article, Comment.new], local: true) do |form| %>

  <p>
  	<%= hidden_field_tag "article_id", @article.id %>
  
    <%= form.text_area :body %>
  </p>
  <p>
    <%= form.submit %>
  </p>
<% end %>