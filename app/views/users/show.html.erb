<header>
	<%= render 'layouts/header' %>
</header>
<br>
<br>
<br>
<%= @user.name %>, <%= @user.email %>

<%= provide(:title, @user.name) %>
<<div class="row">
  <aside class="col-md-5">
    <section class="user_info">
      <h1>
        <%= gravatar_for @user %>
        <%= @user.name %>
      </h1>
    </section>
  </aside>
</div>
<br>
 <%= link_to 'Change your profile',edit_user_path, class: "btn btn-primary" %>